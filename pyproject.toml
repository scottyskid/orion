[tool.pylint.format]
max-line-length = "88"

[tool.isort]
profile = "black"

[tool.black]
#line-length = 99 # override black's default line-length
exclude = '''
/(
    \.git
  | \.mypy_cache
  | \.tox
  | venv
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | \cdk.out
)/
'''